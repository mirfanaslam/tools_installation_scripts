# Following required
app.py
requirements.txt

python --version

clone repo

# create virtual env
python -m venv venv
source venv/bin/activate

pip install -r requirements.txt

python app.py 0.0.0.0:8000

# Past ec2 IP and port
httpe://your-ec2-public-ip:8000
